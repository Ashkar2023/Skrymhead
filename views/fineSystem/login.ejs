<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Fine Logger</title>
    <link rel="icon" type="image/x-icon" href="/images/icons/coin.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@200;500&family=Honk&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
    <style>
        .fs-med{
            font-size: 0.9em;
            font-family: 'Raleway', sans-serif;
        }
        .fs-small{
            /* font-size: ; */
            font-family: 'Raleway', sans-serif;
            font-weight: 200;
        }
        
        .brand{
            font-family: 'Honk', sans-serif;
            font-size: 4rem;
            font-weight: 400;
        }

        .fs-big{
            font-family: 'Raleway', sans-serif;
            font-weight: 500;
            font-size: 2rem;
        }
    </style>
</head>
<body class="bg-black">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-10 text-center">
                <h1 class="brand mt-2 mb-5">Fine Logger</h1>
                
                <form id="loginForm" class="mt-3">
                    <div class="mb-3 text-start">
                        <label for="adminEmail" class=" form-label fs-med text-white">Admin Email</label>
                        <input type="email" class="form-control" id="adminEmail" value="finelogger@brototype.com" name="email">
                    </div>
                    <div class="mb-3 text-start">
                        <label for="adminPassword" class="form-label fs-med text-white">Password</label>
                        <input type="password" class="form-control" id="adminPassword" name="password">
                    </div>
                    <button type="submit" class="bg-transparent focus-ring focus-ring-light btn btn-outline-warning text-warning border-2">
                        <span class="material-symbols-outlined align-text-bottom">
                            monetization_onattach_moneymonetization_onattach_moneymonetization_on
                        </span>
                    </button>
                </form>
            </div>

            <script>
                document.getElementById("loginForm").addEventListener("submit",(event)=>{
                    event.preventDefault();

                    fetch("/fine/login",{
                        method:"POST",
                        headers:{
                            "Content-Type":"application/json",
                        },
                        body:JSON.stringify({
                            email:document.querySelector("input[name='email']").value,
                            password:document.querySelector("input[name='password']").value
                        })
                    })
                    .then(response=>{
                        if(response.ok || response.status===401){
                            return response.json();
                        }
                    })
                    .then(data=>{
                        if(!data.success){
                            Toastify({
                                text: "🤔"+data.message,
                                duration: 2000,
                                gravity: 'top',
                                position: 'right',
                                stopOnFocus: true,
                                style:{
                                    border:"1px solid #FFC000",
                                    borderRadius:"10px",
                                    background:'transparent'
                                },
                                offset:{
                                    y:65
                                }
                            }).showToast();
                        }else{
                            window.location.href=data.url;
                        }
                    })
                    .catch(error=>{
                        console.log(error.message);
                    })

                })
            </script>

        </div>
    </div>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
</body>
</html